<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <link rel="icon" href="/favicon.ico" type="image/x-icon" />
    <link rel="shortcut icon" href="/favicon.ico" type="image/x-icon" />
    <title>MYBLOG</title>
  </head>

  <body>
    <script>
      // console.log("test");
      // app.get("/", function (req, res) {
      //   res.render("articles_main"); // render 파일명(ejs 확장자는 생략이 가능) });
      // });
      window.location.replace("/api/articles");
      // console.log("test");
      // <a href="/article_main">Link to some EJS File</a>;
    </script>
  </body>
</html>
